---
name: sdd-tasks
description: "Create implementation tasks and review workload forecast."
agent: sdd-orchestrator
argument-hint: "<change-name>"
tools: ['agent', 'read', 'search', 'edit']
---

Run the tasks phase through the orchestrator. Include the exact Review Workload Forecast guard lines and suggested work units.

User input: `${input}`
