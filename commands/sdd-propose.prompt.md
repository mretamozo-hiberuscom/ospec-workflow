---
name: sdd-propose
description: "Create or update an SDD proposal."
agent: sdd-orchestrator
argument-hint: "<change-name>"
tools: ['agent', 'read', 'search', 'edit']
---

Run the proposal phase through the orchestrator. Ensure the Capabilities contract, rollback plan, and success criteria are present.

User input: `${input}`
