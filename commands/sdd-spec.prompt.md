---
name: sdd-spec
description: "Write SDD specs for a change."
agent: sdd-orchestrator
argument-hint: "<change-name>"
tools: ['agent', 'read', 'search', 'edit']
---

Run the spec phase through the orchestrator. Read proposal capabilities first and produce new or delta specs with RFC 2119 requirements and Given/When/Then scenarios.

User input: `${input}`
